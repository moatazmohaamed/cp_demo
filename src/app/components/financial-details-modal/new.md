Tasks to do in the existing app:

1- make only 5 order per page (in pagination)
2- make the sub-service is opended by default
3- make the VIP badge under the id instead of next to the name
3- Change the UI of the new order and export and Filter buttons make them smaller that match the size of the rest
3- Add those data in the action mock data and modify the data according the below and also the cs-tasks

Action data: [
    "Awaiting Model\nUpload STL",
    "No Scans Uploaded\nUpload",
    "Update STL\nDownload Scan\nUpload Reg File",
    "Download Scan\nUpload\nFT schedule",
    "Download Scan\nUpdate FT Done",
    "No Actions\nSupported"
]

Cs_task: {
    Assigned,
    Revised,
    Undo
}
and if assigned add any doctor named under the assing
